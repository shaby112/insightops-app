# Optional overrides only. InsightOps ships with built-in defaults,
# so this file can stay untouched for local startup.

INSIGHTOPS_PORT=8080
DB_PASSWORD=InsightOpsDev123
LLM_MODEL=gemini-2.0-flash
DUCKDB_MEMORY_LIMIT=4GB
DUCKDB_THREADS=4
CORS_ORIGINS=
UVICORN_WORKERS=1
UVICORN_LOG_LEVEL=info
RUN_MIGRATIONS_ON_START=true
